region      = "ap-east-1"
name_prefix = "wdl-test"

platform_domain = "*.wdl.sh"
admin_host     = "api.wdl.dev"

# Canonical public site host that should reach the WDL gateway. Terraform also
# covers www.<site_host>; WDL control still owns host declaration and route
# pattern deployment.
site_host = ""

vpc_id = "vpc-xxxxxxxxxxxxxxxxx"

private_subnet_ids = [
  "subnet-xxxxxxxxxxxxxxxxx",
  "subnet-yyyyyyyyyyyyyyyyy",
  "subnet-zzzzzzzzzzzzzzzzz",
]

alb_https_listener_arn = "arn:aws:elasticloadbalancing:ap-east-1:123456789012:listener/app/example/aaaaaaaaaaaaaaaa/bbbbbbbbbbbbbbbb"
alb_security_group_id  = "sg-xxxxxxxxxxxxxxxxx"

d1_test_hooks_enabled = false

# Stateless gateway/user-runtime/system-runtime keep at least one on-demand
# Fargate task and place overflow according to these weights. Stateful runtimes,
# scheduler, and workflows stay on on-demand Fargate.
spot_weight = 3
od_weight   = 1

# Optional assets CDN. Leave both empty to skip CloudFront.
assets_cdn_domain              = ""
assets_cdn_acm_certificate_arn = ""
