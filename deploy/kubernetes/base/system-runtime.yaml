apiVersion: v1
kind: Service
metadata:
  name: system-runtime
  labels:
    app.kubernetes.io/name: wdl
    app.kubernetes.io/component: system-runtime
spec:
  ports:
    - name: loader
      port: 8081
      targetPort: loader
    - name: internal
      port: 8088
      targetPort: internal
    - name: control
      port: 8082
      targetPort: control
  selector:
    app.kubernetes.io/name: wdl
    app.kubernetes.io/component: system-runtime
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: system-runtime
  labels:
    app.kubernetes.io/name: wdl
    app.kubernetes.io/component: system-runtime
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: wdl
      app.kubernetes.io/component: system-runtime
  template:
    metadata:
      labels:
        app.kubernetes.io/name: wdl
        app.kubernetes.io/component: system-runtime
    spec:
      containers:
        - name: redis-proxy
          image: docker.io/getwdl/wdl-rust:latest
          imagePullPolicy: Always
          command: ["/redis-proxy"]
          envFrom:
            - configMapRef:
                name: wdl-config
            - secretRef:
                name: wdl-secrets
          ports:
            - name: redis-proxy
              containerPort: 7070
          readinessProbe:
            exec:
              command: ["/redis-proxy", "healthcheck"]
          livenessProbe:
            exec:
              command: ["/redis-proxy", "healthcheck"]
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
        - name: system-runtime
          image: docker.io/getwdl/wdl-workerd:latest
          imagePullPolicy: Always
          command: ["workerd"]
          args:
            - serve
            - -b
            - /app/dist/workerd-configs/system-runtime.bin
            - --experimental
          envFrom:
            - configMapRef:
                name: wdl-config
            - secretRef:
                name: wdl-secrets
          env:
            - name: REDIS_PROXY_URL
              value: http://127.0.0.1:7070
            - name: AI_REQUEST_MAX_IN_FLIGHT
              value: "32"
            - name: AI_STREAM_MAX_IN_FLIGHT
              value: "16"
            - name: AI_WS_MAX_SESSIONS
              value: "8"
          ports:
            - name: loader
              containerPort: 8081
            - name: internal
              containerPort: 8088
            - name: control
              containerPort: 8082
          readinessProbe:
            httpGet:
              path: /_healthz
              port: internal
          livenessProbe:
            httpGet:
              path: /_healthz
              port: internal
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              memory: 1Gi
